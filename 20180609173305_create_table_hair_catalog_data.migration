#-*- coding:utf-8 -*-
SQL_UP = u"""
CREATE TABLE crawl_photo_data (
  id int(11) NOT NULL auto_increment,
  ref_url varchar(255) default NULL,
  search_word varchar(255) default NULL,
  caption text default NULL,
  photo_url text default NULL,
  photo_data binary default NULL,
  PRIMARY KEY  (id)
);
"""

SQL_DOWN = u"""
DROP TABLE crawl_photo_data;
"""
